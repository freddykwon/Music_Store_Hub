<% layout('/layouts/boilerplate')%>
    <!-- //error stack is outside of bootstrap container -->
    <div class="row">
        <div class="col-6 offset-3">
            <div class="alert alert-danger" role="alert">
                <h2 class="alert-heading">
                    <%=err.message%>
                </h2>
                <% if (process.env.NODE_ENV !=="production" ) { %>
                    <% } %>
            </div>
        </div>
    </div>